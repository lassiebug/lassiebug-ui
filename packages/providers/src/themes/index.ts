export interface Theme {
    // Input
    primary: string;
    secondary: string;
    success: string;
    warning: string;
    danger: string;

    // text
    text: string;
    textContrast: string;
}
